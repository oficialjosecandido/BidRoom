/** Maximum number of bids to keep in memory in the listing-details view. */
export const MAX_DISPLAYED_BIDS = 50;

/** Email validation regex used for guest bid/offer submission. */
export const EMAIL_REGEX = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
